<?php

/**
 * Loaded by PHPUnit before vendor/autoload.php (see vendor/phpunit/phpunit/phpunit).
 * Composer "files" autoload runs after many vendor stubs, so deprecations would still print.
 */
if (PHP_VERSION_ID >= 80400) {
    error_reporting(E_ALL & ~E_DEPRECATED & ~E_USER_DEPRECATED);
}
