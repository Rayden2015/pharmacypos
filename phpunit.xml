<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
    </testsuites>
    <coverage processUncoveredFiles="true">
        <include>
            <directory suffix=".php">./app</directory>
        </include>
    </coverage>
    <!-- Default log channel (stack → daily_env) writes to storage/logs/YYYY-MM-DD-testing.log
         while PHPUnit runs. That is separate from *-local.log (APP_ENV=local: artisan serve, etc.).
         Audit events go to storage/logs/audit-YYYY-MM-DD.log when code uses Log::channel('audit').
         Profile self-service writes profile.updated with section basic|password|security plus user_id,
         site_id, company_id (null for platform super admins), and for basic: email_changed, avatar_changed.
         Email 2FA at login writes auth.two_factor.* (email_code_sent, email_send_failed, email_code_failed,
         email_locked_out, email_resend_limit, email_completed) to the audit channel. -->
    <php>
        <ini name="memory_limit" value="512M"/>
        <server name="APP_ENV" value="testing"/>
        <server name="BCRYPT_ROUNDS" value="4"/>
        <server name="CACHE_DRIVER" value="array"/>
        <server name="DB_CONNECTION" value="sqlite"/>
        <server name="DB_DATABASE" value=":memory:"/>
        <server name="MAIL_MAILER" value="array"/>
        <server name="QUEUE_CONNECTION" value="sync"/>
        <server name="SESSION_DRIVER" value="array"/>
        <server name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
